/**
 * Kodeai MCP Server
 * Allows any AI (Claude, Cursor, Copilot, etc.) to connect and operate the project.
 * Implemented in Phase 10.
 */

export const MCP_VERSION = '0.0.1';

// Placeholder — Phase 10 will implement the full MCP server
export async function startMcpServer(_mode: 'local' | 'remote' = 'local'): Promise<void> {
  console.log(`Kodeai MCP Server v${MCP_VERSION} — not yet implemented`);
}
